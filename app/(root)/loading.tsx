import { Loader2 } from 'lucide-react';

const Loading = () => {
  return (
    <section className="grid content-center">
      <Loader2 className="size-24 animate-pulse" />
    </section>
  );
};

export default Loading;
